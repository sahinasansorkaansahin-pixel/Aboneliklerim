<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />
    <uses-feature android:name="android.hardware.biometrics" android:required="false" />

    <application
        android:name=".AbonelikApp"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:roundIcon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Aboneliklerim">

        <!-- Resolving Manifest Merger conflict between AdMob and Firebase Analytics -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3041412504257891~4090778074"/>

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Aboneliklerim.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar" />

        <activity android:name=".TagsActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar" />
        <activity android:name=".AddEditActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".DetailActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".UpcomingActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ReportsActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar" />
        <activity android:name=".MoreActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar" />
        <activity android:name=".OnboardingActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar" />
        <activity android:name=".PremiumActivity"
            android:theme="@style/Theme.Aboneliklerim.NoActionBar" />

        <receiver android:name=".NotificationReceiver" android:exported="true" />
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver android:name=".SubscriptionWidget" android:exported="true" android:label="@string/regular">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_upcoming_info" />
        </receiver>

        <receiver android:name=".SubscriptionOneTimeWidget" android:exported="true" android:label="@string/one_time">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_upcoming_one_time_info" />
        </receiver>
    </application>

</manifest>
